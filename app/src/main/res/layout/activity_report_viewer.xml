<?xml version="1.0" encoding="utf-8"?>
<!--
    activity_report_viewer.xml

    This layout defines the UI for the ReportViewActivity.
    It includes a search bar for filtering reports, a RecyclerView for displaying report files,
    and a return button for navigation back to the main activity.

    Key Components:
    - EditText for searching reports by name.
    - RecyclerView for displaying the list of reports.
    - Return Button for user navigation.
-->

<LinearLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:orientation="vertical"
    android:padding="16dp"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@drawable/bk">

    <!-- Search bar for filtering reports -->
    <EditText
        android:id="@+id/search_bar"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:hint="Search reports..."
        android:padding="12dp"
        android:inputType="text"
        android:drawableStart="@android:drawable/ic_menu_search"/>

    <!-- RecyclerView for displaying the list of reports -->
    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/report_recycler_view"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1"
        android:scrollbars="vertical"
        android:paddingTop="8dp"/>

    <!-- Return button to go back to the main activity -->
    <Button
        android:id="@+id/buttonreturn"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Return"
        android:layout_marginTop="16dp"
        android:backgroundTint="@android:color/holo_red_light"
        android:textSize="18sp"/>
</LinearLayout>
